/** 주석 방법 /** enter
 * 함수에 대한 설명
 * @param {*} productAmount 제품가격을 숫자로 전달
 * @returns 부가세가 숫자현
 */

function taxAmount(productAmount) {
  // 부가세 10% 가정
  const tax = 0.1;
  return prSoductAmount * tax;
}
